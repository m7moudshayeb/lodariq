# Lodariq pins dependency versions deliberately (PRD §12, §16.0).
# Keep installs reproducible and surface phantom-dependency errors early.
auto-install-peers=true
strict-peer-dependencies=false
save-exact=false
