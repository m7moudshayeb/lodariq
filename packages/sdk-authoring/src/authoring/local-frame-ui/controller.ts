import { ControllerSnapshotFeature } from './controller-snapshot';

/**
 * Stable authoring facade. Capability implementations live in focused modules;
 * this class owns no feature behavior.
 */
export class LocalAuthoringFrameController extends ControllerSnapshotFeature {}
