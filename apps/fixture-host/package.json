{
  "name": "@talmeh/fixture-host",
  "version": "0.0.0",
  "private": true,
  "description": "Realistic SaaS-like host app used as the primary SDK integration + Playwright test surface (PRD §16.0, §16.1).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@talmeh/schema": "workspace:*",
    "@talmeh/sdk-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
